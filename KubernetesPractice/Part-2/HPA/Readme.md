Notes:
- wait a while before it shows up
- kubectl top should show the pod (can be used to ensure metrics server is installed)
- scaling starts once it shows up with kubectl top pods
- requires request/limit resources to have been specified
